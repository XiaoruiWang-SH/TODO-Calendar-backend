spring.application.name=todo-calendar

server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/todocalendar?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=xiaoruiwang
spring.datasource.password=19920503
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
# spring.sql.init.data-locations=classpath:data.sql

# logging.level.org.springframework.jdbc=DEBUG

# Ensure database is created if it doesn't exist
spring.jpa.hibernate.ddl-auto=update

